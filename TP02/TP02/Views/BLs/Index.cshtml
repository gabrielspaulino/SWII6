@model IEnumerable<TP02.Models.BL>

@{
    ViewData["Title"] = "Index";
}

<h1>BLs</h1>

<p>
    <a asp-action="Create">Criar BL</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Consignee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Navio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Consignee)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Navio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Numero">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.Numero">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.Numero">Remover</a>
            </td>
        </tr>
}
    </tbody>
</table>
